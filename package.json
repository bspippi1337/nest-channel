{
  "name": "nest-channel",
  "version": "0.6.0",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 8080",
    "test": "node --test tests/*.test.js",
    "check": "node --check nest-core.js && node --check app.js && node --check github-sync.js && node --check about.js && node --check ux-v06.js && node --check service-worker.js && node --check relay/server.js && node --test tests/*.test.js"
  }
}
