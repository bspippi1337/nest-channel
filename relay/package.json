{
  "name": "nest-channel-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Opaque encrypted WebSocket relay for NEST Channel",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
